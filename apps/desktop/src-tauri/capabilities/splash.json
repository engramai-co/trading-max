{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "splash",
  "description": "Local startup screen only; external pages receive no native capabilities.",
  "windows": ["main", "settings"],
  "permissions": ["core:default"]
}
