interface:
  display_name: "Trading Max Onboarding"
  short_description: "Install and verify Trading Max locally, end to end"
  default_prompt: "Use $trading-max-onboard to set up this cloned checkout completely for local use."
policy:
  allow_implicit_invocation: true
